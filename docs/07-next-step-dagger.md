# Next Step: DAgger

DAgger becomes appropriate after:

- Stroke accuracy is meaningful
- Improvement prediction correlates with Teacher values
- Stop threshold works on held-out Teacher states
- Closed-loop rollout performs at least several useful steps

Only then should Student-visited states be aggregated.
